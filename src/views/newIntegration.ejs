<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Add Integration</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container p-5">
        <h1 class="text-center mb-5 text-uppercase text-muted">Add Integration Form</h1>
        <div class="row">
            <div class="col-sm-5 bg-light border rounded py-3">
                <h2 class="text-center">Login</h2>
                <form action="newIntegration" method="POST">
                    <div class="form-group">
                        <label for="_alias">Alias(Integration Name)</label>
                        <input type="text" name="alias" id="_alias" class="form-control" placeholder="Alias" required>
                    </div>
                    <div class="form-group">
                        <label for="_code"></label>
                        <input type="hidden" name="code" id="_code" class="form-control" placeholder="code" value="<%- code %>"
                            required>
                    </div>
                    
                    <% if (locals.integration_errors) { integration_errors.forEach(function(error_msg){ %>
                        <div class="alert alert-danger" role="alert">
                            <%= error_msg %>
                        </div>
                        <% }); } %>
                            <button type="submit" class="btn btn-primary">Create</button>
                </form>
                
            </div>